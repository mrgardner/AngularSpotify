import { DuplicateTrackPipe } from './duplicate-track.pipe';

describe('DuplicateTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicateTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
