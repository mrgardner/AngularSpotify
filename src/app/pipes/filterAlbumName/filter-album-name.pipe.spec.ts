import { FilterAlbumNamePipe } from './filter-album-name.pipe';

describe('FilterAlbumNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlbumNamePipe();
    expect(pipe).toBeTruthy();
  });
});
