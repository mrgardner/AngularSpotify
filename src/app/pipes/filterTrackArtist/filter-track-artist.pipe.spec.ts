import { FilterTracArtistPipe } from './filter-track-artist.pipe';

describe('FilterTracArtistPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTracArtistPipe();
    expect(pipe).toBeTruthy();
  });
});
